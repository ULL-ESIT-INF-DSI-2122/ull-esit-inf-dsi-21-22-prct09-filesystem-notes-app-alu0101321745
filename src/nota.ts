/**
 * Clase que representa una nota.
 * @param title :
 * @param body :
 * @param color :
 */
export class Nota {
    constructor(public title: string, public body: string, public color: string) {}
}
